# Thin helper (Python) requirements for the Fuzz game per ADR-0001.
# The thin helper computer is a coordinator only: receives final Fuzz + Fresh Clues at end of the Endless Fight from the live fight box,
# constructs the prompt for the Smart Robot (using Training reminder + exact 4 Cleaning Steps), makes one call, parses results, returns them, then immediately forgets everything.
# No private Memory is ever sent or stored. Player rounds use dedicated nvidia-l4; dev iteration uses ZeroGPU/Pro.
# Test harness uses pytest (listed); basic skeleton may exercise with unittest or pytest.
# See parent PRD #1, issue #2, issue #5, handoff, ADR-0001, CONTEXT.md.

fastapi>=0.110
uvicorn[standard]>=0.29
pytest>=8.0
httpx>=0.27

# For Smart Robot one-call (issue #6): huggingface_hub provides InferenceClient.
# Used by ReconstructCoordinator default model_caller (Qwen/Qwen2.5-7B-Instruct).
# DEV: ZeroGPU / Pro / token for iteration (never dedicated nvidia-l4 in dev).
# PROD path (follow-on): dedicated HF Inference Endpoint on nvidia-l4 (hourly, scale-to-zero).
# See validate-smart-robot-on-dev.py, huggingface-zerogpu skill, and ADR-0001.
huggingface_hub>=0.23
